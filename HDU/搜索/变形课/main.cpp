#include <bits/stdc++.h>

using namespace std;

int main()
{
    char s[1000];
    vector<int>ve[26];
    while(~scanf("%s",s))
    {
        if(s[0] == '0')
        {

        }
        ve[s[0]-'a']
    }
    return 0;
}
